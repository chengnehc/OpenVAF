module test
    node a = {input: true, output: true, gnd: false , discipline: Some(Name("electrical"))}
    node b = {input: true, output: false, gnd: false , discipline: Some(Name("electrical"))}
    branch ab1 = Nodes(a, b)
    branch ab2 = Nodes(a, b)
    branch a_gnd = NodeGnd(a)
    branch port_flow_b = PortFlow($root.test.b)
