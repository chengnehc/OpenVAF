[package]
name = "llvm"
version = "0.0.0"
authors = ["DSPOM"]
edition = "2021"
license = "GPL-3.0"
build = "build.rs"

[lib]
doctest = false

[dependencies]
libc = "0.2"

[build-dependencies]
cc = "1.0"
