[package]
name = "mini_harness"
version = "0.0.1"
authors = ["DSPOM"]
edition = "2021"
license = "MIT OR Apache-2.0"
description = "A cargo-nextest compatible test runner that runs tests in a directory"

[lib]
doctest = false

[dependencies]
xflags = "0.3"
